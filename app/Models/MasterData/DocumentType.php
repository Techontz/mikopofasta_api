<?php

declare(strict_types=1);

namespace App\Models\MasterData;

/** Admin-managed lookup list — see MasterDataModel. */
final class DocumentType extends MasterDataModel
{
    protected $table = 'document_types';
}
