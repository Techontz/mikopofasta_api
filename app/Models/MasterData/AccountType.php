<?php

declare(strict_types=1);

namespace App\Models\MasterData;

/** Admin-managed lookup list — see MasterDataModel. */
final class AccountType extends MasterDataModel
{
    protected $table = 'account_types';
}
