<?php

declare(strict_types=1);

namespace App\Models\MasterData;

/** Admin-managed lookup list — see MasterDataModel. */
final class WorkType extends MasterDataModel
{
    protected $table = 'work_types';
}
